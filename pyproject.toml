[project]
name = "capmatch-comms"
version = "0.1.0"
description = "Workspace for CapMatch communication services"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "CapMatch" }
]
dependencies = []

[tool.uv.workspace]
members = [
  "services/email-digest"
]

